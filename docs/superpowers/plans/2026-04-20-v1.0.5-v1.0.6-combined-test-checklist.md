# Android TV v1.0.5 + v1.0.6 — Manual Test Checklist

> **Note (2026-04-20):** v1.0.6 tests (Screensaver Prevention, items 37-40) added to this
> checklist so they can be run together with any v1.0.5 re-verification. Phone/tablet
> sanity checks (items 41-42) moved into their own Mobile App section at the end.
> **Run TV tests directly on the CCwGTV device** — do not test via ws-scrcpy-web until
> the audio-corruption regression there is fixed (see `project_wsscrcpy_audio_regression.md`).

## Pre-Test Setup
- [ ] Uninstall previous version from Android TV device
- [ ] Sideload the new APK
- [ ] Have at least one audiobook library with 10+ items
- [ ] Have at least one podcast library with episodes

---

## H1 Fix: Connect Page TV Layout
- [ ] 1. Fresh launch (no saved server): App opens on connect page with TV-appropriate layout (not phone layout)
- [ ] 2. D-pad works on connect page: Can navigate server URL field, submit button, saved server entries with D-pad
- [ ] 3. Green focus ring visible on all focusable elements on the connect page
- [ ] 4. After connecting: Navigate to library, verify TV layout persists

## H2 Fix: Navigation Never Gets Stuck
- [ ] 5. Rapid ArrowDown: On a bookshelf with 20+ items, hold ArrowDown for 5+ seconds — focus smoothly scrolls through rows without getting stuck
- [ ] 6. Rapid ArrowUp: Same test, hold ArrowUp — focus scrolls back up without getting stuck
- [ ] 7. Focus recovery after rapid nav: After rapid scrolling, wait 2 seconds, then click a card — app should not be in a dead state
- [ ] 8. Long session stability: Navigate around for 2+ minutes — no permanent loss of D-pad navigation

## H3 Fix: Scroll Position Reset on Tab Switch + Nav History
- [ ] 9. Library tab switch: Scroll down in Library tab, navigate to Home via nav bar — Home should be at the top
- [ ] 10. Home tab switch: Scroll down in Home, switch to Library — Library should be at the top
- [ ] 11. Main → Main always resets: On Library scroll down + focus a card, nav to Home, press remote Back — Library reloads at the top with first-card focus (fingerprint intentionally cleared on every Main→Main transition, whether forward or back)
- [ ] 12. Detail → Main → Back: From Library, focus a book card and press Enter; on the book detail page press remote Back — focus returns to that exact card
- [ ] 13. On-screen back arrow = remote Back: Repeat test 12 but use the top-left on-screen back arrow instead of the remote Back — identical restoration
- [ ] 14. Detail → Detail → Back preserves intermediate: From Playlists tab, open a playlist; focus a specific book row's ellipsis (⋮); press Enter to open History / More info; press remote Back — returns to the playlist with focus on that row's ellipsis (not the first item, not the primary play button). NOTE: playlist rows do not have a book-detail entry path — only play button and ellipsis — so ellipsis is the canonical test for this design.
- [ ] 15. Author bio fresh-start on re-entry: From Authors tab, enter an author's bio page and scroll/focus something mid-page; press Back to return to Authors list; re-enter the same author — page starts at the top, focus on the page title area, not where you left off
- [ ] 16. Playlist row play-button fingerprint (KNOWN REGRESSION, fix post-v1.0.6): From Playlists tab, open a playlist; focus a specific book row's play button (not the playlist's primary play); press Enter — fullscreen player expands and playback starts; exit the player back to the playlist — focus SHOULD return to that row's play button. Currently lands on the playlist's primary play button. Tracked in `project_tv_playlist_play_button_regression.md`.

## H4 Fix: No Double-Fire on Enter
- [ ] 17. Book card Enter: Focus a book card, press Enter once — navigates to detail page once (no flash/double-navigate)
- [ ] 18. Author card Enter: Focus an author card, press Enter — navigates to author page once
- [ ] 19. Series card Enter: Focus a series card, press Enter — navigates once
- [ ] 20. Collection card Enter: Focus a collection card, press Enter — navigates once
- [ ] 21. Playlist card Enter: Focus a playlist card, press Enter — navigates once
- [ ] 22. Library modal: Open library selector, press Enter on a library — switches once, no double-switch
- [ ] 23. Side drawer: Open hamburger menu, press Enter on a menu item — navigates once
- [ ] 24. Toolbar icons: Focus filter/sort/view icons on bookshelf toolbar, press Enter — opens modal once

## M3+M4 Fix: No Focus Flash on Navigation
- [ ] 25. Library change: Switch libraries (if multiple) — focus lands on first card cleanly, no jumping
- [ ] 26. Fast navigation: Rapidly press Enter on a card then immediately press Back — focus restores cleanly without visible flash/jump
- [ ] 27. Filter/sort change: Apply a filter or sort on bookshelf toolbar — focus lands on first card, no erratic movement

## M10+M14 Fix: Player Controls + Focus Visibility

NOTE: Two former items (mini player D-pad isolation, and fullscreen collapse-then-re-expand)
were **removed** from this checklist as invalid — the mini player UI was retired in an
earlier release. Current design: audio player is fullscreen-only, and "collapse" on
fullscreen closes the player rather than minimizing it. Any future QA pass that
re-introduces a mini player should re-add those tests.

- [ ] 28. Fullscreen player: Expand player to fullscreen — all controls focusable with green rings
- [ ] 29. Fullscreen ellipsis → History does NOT collapse player (KNOWN REGRESSION, fix post-v1.0.6): Start playback so fullscreen player is active; open the fullscreen ellipsis (⋮) menu; select "History" — the app navigates to the History page and the player unexpectedly minimizes into the (retired) mini-player state. Expected: History opens without breaking fullscreen-only player invariant. Preferred fix: disable the History option in the fullscreen ellipsis menu so there's no route change while playing. Tracked in `project_tv_fullscreen_history_regression.md`.

## Regression Tests
- [ ] 30. Spatial navigation: Left/Right stays in row, Up/Down moves between rows
- [ ] 31. Focus ring: Green border/outline visible on all focused elements
- [ ] 32. Back button focus restore: Navigate into a book detail, press Back — focus returns to the card you came from
- [ ] 33. Overlay trapping: Open a modal (e.g., filter) — D-pad is trapped within the modal
- [ ] 34. Side drawer trapping: Open hamburger — D-pad is trapped within drawer
- [ ] 35. Audio player fullscreen: Start playback — player auto-expands, all controls navigable
- [ ] 36. App exit: Press Back on home screen — confirmation dialog appears, confirming exits the app fully

---

## v1.0.6 Fix: Screensaver Prevention During Active Playback (TV-only)

Addresses user-reported issue where CCwGTV Ambient Mode screensaver interrupted audio playback and returned users to the Home screen after ~30 min idle. Screen-wake lock is now held while audio is playing and released on every session-end path.

- [ ] 37. Screen stays awake during playback: Start a book playing, leave the TV alone for 12+ minutes (past the 10-min Ambient Mode threshold). Screen should stay on; Ambient Mode should NOT engage; audio should continue uninterrupted.
- [ ] 38. Pause releases the wake lock: Pause playback. Screen should dim normally after the idle timeout and Ambient Mode should engage as expected (standard Android TV behavior).
- [ ] 39. Close player releases the wake lock: Resume playback, then close the player (menu > Close, or TV Back / fullscreen-collapse on TV). With no playback active, Ambient Mode should engage normally after idle.
- [ ] 40. Playback failure / force-kill: Start playback, then force-stop the app (Settings > Apps > Audiobookshelf > Force Stop, or kill via adb). No orphaned wake lock should remain — verify Ambient Mode still engages after idle on the next app launch.

---

## Mobile App Tests (phone/tablet)

Run these on a real Android phone or tablet after the TV checklist is complete.
Both verify the TV-specific additions have **zero** effect on non-TV devices —
phone/tablet behavior should be identical to before the TV changes.

- [ ] 41. Phone modal close: Install the same APK on a phone; from any bookshelf, scroll down, open and close a modal — page should NOT auto-scroll to reveal a card. (Was M2 #20.)
- [ ] 42. Phone screensaver sanity: On the same phone, start and stop audio playback. Screen should dim/sleep normally during playback — the v1.0.6 keep-awake logic must be fully TV-gated and have zero effect on mobile. (Was v1.0.6 #38.)
